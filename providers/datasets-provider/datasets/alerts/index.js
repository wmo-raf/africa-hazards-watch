import weatherAlerts from "./weather-alerts";
import tropicalCylones from "./tropical-cylones";

const datasets = [...weatherAlerts.datasets];
const updates = [...weatherAlerts.updates];

export default { datasets, updates };
